# SMAP 手机版（SMAP-Android）开发交接文档

> 交接时间：2026-08-18
> 交接人：DeepSeek 开发代理
> 接手人：Claude（请先通读本文档再动手）
> 项目代码：`F:\编程文件\编程计划\SMAP-Android`
> 真机：vivo X100s Pro（V2403A，序列号 10AE7H0MFK0016G）

---

## 1. 项目定位

把桌面版 SMAP（光遇自动弹琴助手，C# WPF）移植到 Android 手机。光遇本身是手游，手机端直接弹琴是更主流场景。

### 1.1 用户明确的设计约束（必须遵守）

1. **横屏**：App 锁定横屏（`sensorLandscape`），自动弹琴场景是横屏使用。
2. **风格不脱离桌面版**：深蓝紫渐变背景、圆角、音乐软件式布局、青色强调色（`#7DD3FC`）。参考桌面版 v2.x 的音乐软件界面（左侧边栏 + 中栏曲库 + 底部播放条）。
3. **交互模式（用户拍板）**：主界面有一个 **"游戏演奏"按钮** → 点击后进入**悬浮球模式**（一个小圆圈常驻屏幕、可拖动）→ 点击悬浮球 → 展开**悬浮面板**（选曲、播放控制）→ 播放时后台自动弹琴。用户切到光遇即可用。

### 1.2 核心功能

- 自动弹琴：无障碍服务 `dispatchGesture` 按屏幕坐标模拟点击琴键
- 曲库：支持 **JSON / TXT / MIDI** 三种格式（已完成）
- 在线曲库：缪斯树屋（musetreehouse.com）API（未开始）
- 练习/读谱模式（未开始）

---

## 2. 技术栈与环境

| 组件 | 位置 / 版本 |
|---|---|
| Kotlin + Jetpack Compose + Material3 | compose-bom 2025.03.01 |
| AGP | 8.9.2（Gradle 8.13） |
| JDK | `C:\Users\lingy\jdk21\jdk-21.0.11+10`（Temurin 21） |
| Android SDK | `D:\DSH_Works\android-sdk`（platform-tools / platforms;android-35 / build-tools;35.0.0，licenses 已接受） |
| Gradle | `D:\DSH_Works\gradle-8.13` |
| Android Studio | `D:\Android\Android Studio`（2026.1，用户自装） |
| adb | `D:\DSH_Works\android-sdk\platform-tools\adb.exe` |
| compileSdk/targetSdk/minSdk | 35 / 35 / 26 |

### 2.1 构建方法

```powershell
# 一键构建（脚本已固化所有环境变量和坑规避）
powershell -File "F:\编程文件\编程计划\SMAP-Android\build.ps1"

# 或手动（注意：必须从工作区运行 + -p 指定项目，见"踩坑"第 3 条）
cd D:\DSH_Works
$env:JAVA_HOME = "C:\Users\lingy\jdk21\jdk-21.0.11+10"
$env:GRADLE_USER_HOME = "D:\DSH_Works\.gradle"
D:\DSH_Works\gradle-8.13\bin\gradle.bat -p "F:\编程文件\编程计划\SMAP-Android" :app:assembleDebug
```

**APK 输出**：`D:\DSH_Works\smap-android-build\app\outputs\apk\debug\app-debug.apk`（构建产物被重定向到工作区，不在项目目录内）

### 2.2 真机安装测试

```powershell
$adb = "D:\DSH_Works\android-sdk\platform-tools\adb.exe"
adb install -r <apk路径>          # 安装（vivo 会弹权限确认，需用户点允许）
adb shell am start -n com.smap.android/.MainActivity   # 启动
adb shell uiautomator dump /sdcard/ui.xml && adb shell cat /sdcard/ui.xml   # UI 文本验证
adb shell "dumpsys window windows" | findstr "2038"    # 验证悬浮窗（type=2038=APPLICATION_OVERLAY）
```

⚠️ **uiautomator dump 看不到悬浮窗内容**，验证悬浮窗必须用 `dumpsys window`。

---

## 3. 项目结构

```
F:\编程文件\编程计划\SMAP-Android\
├── settings.gradle.kts        # problems 禁用（沙箱）
├── build.gradle.kts           # AGP 8.9.2 / Kotlin 2.1.10 / buildDir 重定向到工作区
├── gradle.properties          # projectcachedir / kotlin in-process / overridePathCheck
├── local.properties           # sdk.dir=D:\DSH_Works\android-sdk
├── build.ps1                  # 一键构建（推荐使用）
├── README.md
└── app/src/main/
    ├── AndroidManifest.xml    # 横屏 + 无障碍服务 + 悬浮服务（specialUse 前台服务）+ 悬浮窗权限
    ├── assets/songs/          # 9 首示例曲谱：6 个 txt（Sky JSON）+ 3 个 mid
    ├── java/com/smap/android/
    │   ├── MainActivity.kt                     # 主界面：曲库列表 + "🎮 游戏演奏"按钮 + 悬浮窗权限引导
    │   ├── model/SongModels.kt                 # SkySong 数据模型 + JSON 数组解析（org.json）
    │   ├── data/SongRepository.kt              # 曲库读取：BOM 检测（UTF-8/16）+ 按扩展名分流
    │   ├── midi/MidiParser.kt                  # SMF 解析器（零依赖：MThd/MTrk/变长量/running status/tempo）
    │   ├── midi/MidiImporter.kt                # MIDI→SkySong（tempo map→tick→ms、自动移调、黑键方向吸附）
    │   ├── engine/PlayerEngine.kt              # 播放引擎：协程按时间线 dispatchGesture 点击
    │   ├── engine/KeyLayout.kt                 # 琴键布局模型 + SharedPreferences 持久化
    │   ├── service/SMAPAccessibilityService.kt # 无障碍服务（dispatchGesture tap）
    │   └── service/FloatService.kt             # 悬浮球 + 选曲面板（传统 View 实现！）
    └── res/xml/accessibility_service_config.xml
```

---

## 4. 已完成 / 未完成

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M1 环境+骨架 | ✅ | 构建链路全通，APK 9MB |
| M2 曲谱解析+曲库 | ✅ | JSON/TXT（含 UTF-16）/MIDI 三种格式，真机验证 9 首全加载 |
| M3 在线曲库 | ❌ | 未开始（API 见第 7 节） |
| M4 自动弹琴 | 🟡 部分 | 无障碍服务✅ / 播放引擎✅ / 琴键布局模型✅ / 悬浮球+面板✅；**琴键校准 UI 未做**、**真机光遇实测未做** |
| M5 练习/读谱 | ❌ | 未开始 |
| M6 发布 | ❌ | 未开始 |

### 当前可运行状态
- 主界面：曲库列表（横屏、深蓝紫渐变风格）、点歌曲进演奏界面（早期版本遗留，功能被悬浮球取代，可保留或删除）
- **"🎮 游戏演奏"按钮 → 悬浮球出现（左上角 🎹，可拖动）→ 点击展开底部选曲面板 → 选曲点"▶ 开始弹奏" → 无障碍点击琴键**
- 悬浮球/面板已验证能显示（窗口 type=2038 存在），但**自动弹琴在光遇里的实际效果未验证**（琴键坐标是默认屏幕比例，大概率需要校准）

---

## 5. ⚠️ 踩坑记录（接手前必读，避免重蹈覆辙）

### 5.1 本机网络环境
- **Windows schannel TLS 栈坏了**：`SEC_E_NO_CREDENTIALS (0x8009030e)`，git/curl/Invoke-WebRequest/winget 全部 HTTPS 失败
- **Node.js 和 Java 的 TLS 正常**（走 OpenSSL/JSSE）→ 所有下载用 Node 脚本或 Java 工具
- 系统代理配置 `127.0.0.1:7897`（Clash）但代理未运行 → sdkmanager 走系统代理失败；已用 Node 手动下载 SDK 组件绕过

### 5.2 沙箱写限制（若接手环境仍受限）
- 沙箱只允许写 `D:\DSH_Works` 工作区，F 盘项目目录的**写入需授权**（写源码时）
- **Gradle 进程只能写工作区** → 以下全部重定向已在配置里固化：
  - `build.gradle.kts`：`allprojects { layout.buildDirectory.set(File("D:/DSH_Works/smap-android-build/${project.name}")) }`
  - `gradle.properties`：`org.gradle.projectcachedir=D:/DSH_Works/gradle-project-cache`、`kotlin.project.persistent.dir=D:/DSH_Works/kotlin-persistent`、`kotlin.compiler.execution.strategy=in-process`
  - **构建必须从 `D:\DSH_Works` 目录运行 + `-p` 指定项目**，否则 problems report 写 F 盘失败
  - Debug 签名：`D:\DSH_Works\android-sdk\debug.keystore`（AGP 默认 `~/.android/debug.keystore` 不可写），signingConfigs 已配好

### 5.3 项目路径中文
- `F:\编程文件\编程计划\...` 含中文 → AGP 默认拒绝，`android.overridePathCheck=true` 已加

### 5.4 ❗悬浮窗用 ComposeView 会崩溃（本开发最大的坑）
- 初版悬浮球/面板用 ComposeView 添加到 WindowManager：
  1. 报 `ViewTreeLifecycleOwner not found` → 需给 ComposeView 设置 LifecycleOwner/ViewModelStoreOwner/SavedStateRegistryOwner
  2. Service 实现 `SavedStateRegistryController` 报 `Restarter must be created only during owner's initialization stage`
  3. 去掉 SavedStateRegistry 后 Compose 1.7 又报 `Composed into the View which doesn't propagate ViewTreeSavedStateRegistryOwner`
- **结论：悬浮窗 UI 不要用 ComposeView，用传统 View**（TextView/LinearLayout/ListView）。`FloatService.kt` 已用传统 View 重写并验证可显示。

### 5.5 vivo 系统特性
- **`startForegroundService` 被 vivo 拦截**（服务不启动）→ 改用 `startService`（App 前台时允许）即可
- 安装 APK 会弹权限确认，**用户需手动点允许**（`INSTALL_FAILED_ABORTED: User rejected permissions`）
- 悬浮窗需要 `SYSTEM_ALERT_WINDOW` 授权（已授予）；vivo 可能还要求"后台弹出界面"权限（未验证）
- 无障碍服务需要在系统设置手动开启（用户已完成）

### 5.6 曲谱编码
- 桌面版曲谱一部分是 **UTF-16 LE（BOM FF FE）** → `SongRepository.decodeText()` 按 BOM 自动识别（UTF-16LE/BE、UTF-8 BOM、默认 UTF-8）

### 5.7 UI 验证限制
- 当前模型**无法查看截图**（无图像输入能力）→ 所有界面验证用 `uiautomator dump`（文本）+ `dumpsys window`（窗口层级）
- 接手者若能看图，建议用截图验证 UI 视觉效果（`adb exec-out screencap -p > screen.png`）

---

## 6. 关键实现细节

### 6.1 曲谱格式（Sky 标准）
```json
[{
  "name": "曲名", "bpm": 200, "bitsPerPage": 16, "pitchLevel": 0,
  "isComposed": false, "isEncrypted": false, "keyCount": 15,
  "songNotes": [ {"time": 1471, "key": "1Key2"}, ... ]
}]
```
- `time` 毫秒，`key` 光遇琴键索引 0~14（15 键 = C4~C6 白键：60/62/64/65/67/69/71/72/74/76/77/79/81/83/84）
- 桌面版曲谱目录：`F:\编程文件\编程计划\SMAP-WPF\曲谱库\`（几百首，含 UTF-16）

### 6.2 自动弹琴链路
1. `MainActivity` 点"🎮 游戏演奏" → 检查 `Settings.canDrawOverlays` → 启动 `FloatService`
2. `FloatService.onCreate` → 加载曲库 + 琴键布局 → 加悬浮球（TextView，拖动+点击）
3. 悬浮球点击 → `showPanel()`（ListView 选曲 + 播放按钮）
4. 播放 → `PlayerEngine.play(song, keys, screenW, screenH)`：协程里按音符 time 排序逐个 `delay` 后调 `SMAPAccessibilityService.tap(x, y)`（`dispatchGesture`，按住 18ms）
5. 琴键坐标：`KeyLayout.computeKeys()` 按屏幕比例（3 行 5 列，中心偏下），默认 `panelY=0.12, keyW=0.15, keyH=0.10, rowGap=0.03`

### 6.3 MIDI 导入逻辑（移植桌面版 MidiImporter）
- SMF 解析：MThd（format/ntrks/division）+ MTrk 事件流（变长 delta、running status、meta tempo 0xFF 0x51）
- tempo map → tick→ms（缺省 500000μs=120BPM）
- 自动移调：0~11 半音里选白键率最高（>6 取负），中位音八度居中到 C5(72)
- 黑键吸附：按旋律方向（当前 vs 前一个音高）取上/下白键，边界回退

### 6.4 悬浮窗权限流程
- Manifest：`SYSTEM_ALERT_WINDOW` + `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_SPECIAL_USE`
- FloatService：`foregroundServiceType="specialUse"` + `<property PROPERTY_SPECIAL_USE_FGS_SUBTYPE>`
- 主界面按钮：无权限 → 跳 `Settings.ACTION_MANAGE_OVERLAY_PERMISSION`；有权限 → `startService`

---

## 7. 待办（接手优先级建议）

### P0（核心链路完善）
1. **琴键校准 UI**：`KeyLayout` 模型和持久化已有，缺 UI。建议在悬浮面板加"校准"入口：显示琴键预览 + 偏移/缩放微调（←→↑↓ + 键大小 + 行距）+ 保存/重置。参考旧版 `ui/PerformScreen.kt` 里的 `CalibrationPanel`（已被悬浮球取代但可复用逻辑）
2. **真机光遇实测**：让用户进光遇弹琴界面，开自动弹琴，确认默认坐标是否对准琴键，不对则用校准调整
3. **播放中停止入口**：当前面板选曲播放后面板收起，用户想停止只能再点悬浮球重新开面板（可用），但建议悬浮球在播放中变红/变图标提示状态

### P1（功能完善）
4. M3 在线曲库：复用缪斯树屋 API（桌面版已在用）：
   - 登录 `POST musetreehouse.com/api/game_login.php` {username,password} → {success, user:{id,username,mid}}
   - 列表 `GET /api/sheets/list.php?per_page&page&sort=newest|hot|downloads&q&difficulty`
   - 下载 `GET <download_url>`；上传 `POST /api/sheets/upload.php`（multipart）
   - 桌面版实现参考 `F:\编程文件\编程计划\SMAP-WPF\CloudApi.cs`
5. 无障碍权限引导：主界面检测未开启时提示（当前只有演奏时静默失败）
6. 主界面"设置"页（侧边栏占位）：语言/主题/琴键布局重置/关于

### P2（体验）
7. M5 练习/读谱模式（触屏大键盘，参考桌面版 project_smap_practice.md 记忆）
8. 音色试听（打包采样 wav 或合成音）
9. M6 发布：正式签名 keystore、版本号、README、GitHub Release

---

## 8. 用户对开发方式的期望（重要）

接手后请特别注意用户的反馈：
1. **拓展思维**：用户明确批评"只限于我说的而不知道拓展思维"——做完用户要求的功能后，主动补齐周边体验（错误处理、状态反馈、权限引导、边界情况），不要只做字面要求
2. **少试错**：用户批评"试错太多"——动手前先完整想清楚方案（尤其 Android 悬浮窗、无障碍、vivo 系统这些坑多的领域），参考本文档踩坑记录，避免反复迭代
3. **视觉验证**：接手者若能看图，务必用截图验证 UI（用户很在意风格：横屏 + 深蓝紫渐变 + 圆角 + 音乐软件式）
4. 用户真机是 **vivo X100s Pro**，所有交互最终以真机为准

---

## 9. 其他

- 桌面版 SMAP 代码：`F:\编程文件\编程计划\SMAP-WPF`（MidiImporter.cs / CloudApi.cs / SongLibrary.cs 可参考移植）
- 桌面版开发记忆（详细需求/踩坑/风格）：`C:\Users\lingy\.claude\projects\C--Users-lingy\memory\` 下的 `project_skymusic.md` / `project_smap_practice.md` / `reference_smap_release.md` / `reference_smap_midi_import.md`
- 测试 MIDI：`F:\UserDownload\SMAP偏移测试\`（10 个不同调性/音域测试文件）
- 旧演奏界面（`ui/PerformScreen.kt`，Compose 版）功能上已被悬浮球取代，其"校准面板"逻辑可复用

**祝接手顺利。**
